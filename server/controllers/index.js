const asset_types = require('./asset_types');
const it_assets = require('./it_assets');
const it_asset_inventory = require('./it_asset_inventory');
const employees=require("./employees")
const employee_assets=require("./employee_assets")

module.exports = {
    asset_types,
    it_assets,
    it_asset_inventory,
    employees,
    employee_assets
};